import { mockButtonResponse, mockCardResponse, mockNavbarResponse } from "../negative/fixtures/mock-responses.js";

export const fixtures = {
  button: mockButtonResponse,
  card: mockCardResponse,
  navbar: mockNavbarResponse
};
